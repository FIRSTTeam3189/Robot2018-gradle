/*#include "AddTwo.h"

AddTwo::AddTwo() {
	Requires(CommandBase::vision.get());
	// Use Requires() here to declare subsystem dependencies
	// eg. Requires(Robot::chassis.get());
}

// Called just before this Command runs the first time
void AddTwo::Initialize() {

}

// Called repeatedly when this Command is scheduled to run
void AddTwo::Execute() {
	/*
	int number = SmartDashboard::GetNumber("input number", 0);
	CommandBase::vision->SendData((char *) &number, sizeof(number));
	CommandBase::vision->ReceiveData((char *) &number, sizeof(number));
	SmartDashboard::PutNumber("return number", number);
	comment end was here
}

// Make this return true when this Command no longer needs to run execute()
bool AddTwo::IsFinished() {
	return false;
}

// Called once after isFinished returns true
void AddTwo::End() {

}

// Called when another command which requires one or more of the same
// subsystems is scheduled to run
void AddTwo::Interrupted() {

}
*/
